<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.example.maven-samples</groupId>
  <artifactId>single-module-project</artifactId>
  <packaging>jar</packaging>
  <version>1.0-SNAPSHOT</version>
  <name>A Single Maven Module</name>
  <description>Sample single module Maven project with a working, deployable site.</description>
  <url>http://www.example.com</url>

  <properties>
    <project.build.sourceEncoding>utf-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>utf-8</project.reporting.outputEncoding>
  </properties>

  <distributionManagement>
    <site>
      <id>site-server</id>
      <name>Test Project Site</name>
      <url>file:///tmp/single-module-site</url>
    </site>
  </distributionManagement>

  <build>
    <finalName>${project.artifactId}</finalName>

    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version> <!-- Updated to a newer version -->
        <configuration>
          <source>1.8</source> <!-- Updated to Java 8 source compatibility level -->
          <target>1.8</target> <!-- Updated to Java 8 target compatibility level -->
        </configuration>
      </plugin>

      <!-- Add these test dependencies -->
      <plugin>
        <artifactId>maven-surefire-plugin</artifactId>
        <version>2.22.2</version>
      </plugin>

      <plugin>
        <artifactId>maven-failsafe-plugin</artifactId>
        <version>2.22.2</version>
      </plugin>

      <!-- The rest of your existing plugins -->

    </plugins>
  </build>

  <dependencies>
    <!-- ... (your existing dependencies) -->

    <!-- Add these test dependencies -->
    <dependency>
      <groupId>junit</groupId>
      <artifactId>junit</artifactId>
      <version>4.10</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <version>1.8.5</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.hamcrest</groupId>
      <artifactId>hamcrest-core</artifactId>
      <version>1.3</version>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <distributionManagement>
    <site>
      <id>site-server</id>
      <name>Test Project Site</name>
      <url>file:///tmp/single-module-site</url>
    </site>
  </distributionManagement>

  <build>
    <finalName>${project.artifactId}</finalName>

    <plugins>
      <plugin>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>3.8.1</version> <!-- Updated to a newer version -->
        <configuration>
          <source>1.8</source> <!-- Updated to Java 8 source compatibility level -->
          <target>1.8</target> <!-- Updated to Java 8 target compatibility level -->
        </configuration>
      </plugin>

      <plugin>  
        <!-- Build an executable JAR -->  
        <groupId>org.apache.maven.plugins</groupId>  
        <artifactId>maven-jar-plugin</artifactId>  
        <version>3.1.0</version>  
        <configuration>  
          <archive>  
            <manifest>  
              <mainClass>com.example.Greeter</mainClass>  
            </manifest>  
          </archive>
        </configuration>
      </plugin> 

      <!-- The rest of your existing plugins -->

    </plugins>
  </build>

  <!-- Your existing dependencies, SCM configuration, and other sections -->

  <prerequisites>
    <maven>3.0.3</maven>
  </prerequisites>
</project>
